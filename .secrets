# Secrets
# These contain references to 1Password items. To resolve them, install the 1Password CLI
# Prepend any command with `op run --` to resolve the secrets before execution

# export OPENAI_API_KEY="op://Personal/ho2uz4uircjfpiugj2u6h7f3ue/api key"
# export GHSTACK_OAUTH_TOKEN="op://Personal/Master GitHub Personal Access Token/token"
